{
  "name": "dona",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "migrate": "pnpm --filter api migrate"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}